describe("#currentandBacklogDataProvider", () => {
  describe('getChildren', () => {
    it('Should return an  array of all stories from current and backlog iterations', () => {

    })

    it('Should return an empty array if no iterations exist', () => {

    })
  })

  describe('getTreeItem', () => {
    it('Should return a TreeItem with the story name as the label', () => {

    })

    it('Should return a TreeItem with a command to start working on the story', () => {

    })

    it('Should have add blocker, add task, estimate')
  })
})
